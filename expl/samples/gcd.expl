int main()
{
    decl
        int a,b,temp;
        str lol;
    enddecl
    begin
        lol = "enter the numbers here";
        write(lol);
        read(a);
        read(b);
        while(a != b) do
            if(a > b)then
                a = a-b;
            else
                b = b-a;
            endif;
        endwhile;
        write(a);
        return 0;
    end
}