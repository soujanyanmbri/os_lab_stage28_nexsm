int main()
{
decl
    int temp,counter;
enddecl
begin
	counter = 0;
    while (counter < 16) do
		temp=exposcall("Getuname",counter); 
		if (temp != -1) then   
			temp=exposcall("Write",-2,temp);
		endif;
		counter = counter + 1;
	endwhile;
    return 0;
end
}